{
  "name": "chat2local",
  "version": "1.0.0",
  "description": "ChatGPT web to local machine bridge via official Secure MCP Tunnel - codex exec, aside exec/repl, codex-like tools",
  "type": "module",
  "scripts": {
    "start": "bun run bin/mcp.ts",
    "serve": "bun run bin/serve.ts",
    "setup": "bun run bin/setup.ts",
    "install": "bun install"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0"
  }
}
